@model IEnumerable<ModelEF.Model.DanhMuc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DanhMuc_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.DanhMuc_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DanhMuc_Id })
        </td>
    </tr>
}

</table>
